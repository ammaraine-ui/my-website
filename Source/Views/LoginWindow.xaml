<Window x:Class="AmmarAlani.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تسجيل الدخول - نظام عمار العاني"
        Height="500" Width="850"
        FlowDirection="RightToLeft"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" CornerRadius="16" Padding="24" Margin="0,0,15,0" Background="#F5F7FB">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="نظام عمار العاني" FontSize="28" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBlock Text="الإدارة والمبيعات والمخازن والحسابات" FontSize="16"/>
                <TextBlock Text="نسخة الشبكة" Margin="0,16,0,0" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" CornerRadius="16" Padding="30" Background="White" BorderBrush="#E3E7EF" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="تسجيل الدخول" FontSize="26" FontWeight="Bold" Margin="0,0,0,20"/>
                <TextBlock Text="اسم المستخدم"/>
                <TextBox Height="38" Margin="0,6,0,14"/>
                <TextBlock Text="كلمة المرور"/>
                <PasswordBox Height="38" Margin="0,6,0,14"/>
                <TextBlock Text="الفرع"/>
                <ComboBox Height="38" Margin="0,6,0,14"/>
                <CheckBox Content="تذكر بيانات الدخول" Margin="0,0,0,18"/>
                <Button Content="دخول" Height="42"/>
                <TextBlock Text="حالة السيرفر: جاهز" Margin="0,20,0,0" Foreground="Green"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
